# Copia esto a .env (que no se sube al repo) y pon un valor real antes de "docker compose up".
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# Cambiarlo más adelante cierra la sesión de todo el mundo (las cookies dejan de verificar).
SESSION_SECRET=
